const militaryConfig = {
  projectId: process.env.MILITARY_SANITY_PROJECT_ID,
  dataset: process.env.MILITARY_SANITY_DATASET,
  apiVersion: '2024-08-18',
  useCdn: false,
};

export default militaryConfig;
